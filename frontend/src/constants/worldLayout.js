/**
 * Shared layout tokens so Professional / Personal / Emotional read as one system
 * with different palettes, not three unrelated sites.
 */
export const worldContentClass =
  "relative z-10 px-6 md:px-12 lg:px-20 pb-40 max-w-[1400px] mx-auto";

export const worldTopBarClass =
  "relative z-10 flex justify-between items-center px-6 md:px-12 py-6 font-mono text-[10px] md:text-xs uppercase tracking-[0.3em]";
